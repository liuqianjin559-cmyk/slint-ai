// See https://go.microsoft.com/fwlink/?LinkId=733558
// for the documentation about the tasks.json format
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile extension",
            "type": "shell",
            "command": "node",
            "args": ["${workspaceFolder}/compile-desktop.js"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
